const defaultState = {isRegisted:false,isLoggedin:false };

export default function authReducer(state = defaultState, action) {
    switch (action.type) {
        default:
            return state;
    }
}